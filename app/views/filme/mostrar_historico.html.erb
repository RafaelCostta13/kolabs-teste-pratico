<div class="container">
	<table class="table">
		<tr>
			<th>Termo de pesquisa</th>
			<th>Data da pesquisa</th>
		</tr>
		<% @historico.each do |h| %>
			<tr>
				<td><%= h.termo_pesquisa %></td>
				<td><%= h.created_at %></td>
			</tr>
		<% end %>
	</table>
</div>